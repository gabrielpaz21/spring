version: '3.1'

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: spring_data_jpa
      MYSQL_USER: gabriel
      MYSQL_PASSWORD: Aa--Gg||27
    ports:
      - "3306:3306"
